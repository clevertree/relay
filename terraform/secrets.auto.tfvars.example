# Example secrets file for Terraform (do NOT commit the real secrets)
# Copy this to `terraform/secrets.auto.tfvars` and fill in values.

# Vercel API token used by the Docker entrypoint to upsert DNS records
vercel_api_token = "REPLACE_WITH_YOUR_VERCEL_API_TOKEN"

# Optional: Rackspace Spot token if not using ../../rackspace_token file
# rackspace_spot_token = "REPLACE_WITH_YOUR_RACKSPACE_TOKEN"
