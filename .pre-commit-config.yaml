# Pre-commit configuration: run workspace tests
repos:
  - repo: local
    hooks:
      - id: run-tests
        name: Run workspace tests
        entry: pnpm -w test
        language: system
        stages: [commit]
