import { styled } from './utils/tailwindStyled'
import { tailwindToStyle, registerThemeStyles } from './utils/tailwindMapper'

export { styled, tailwindToStyle, registerThemeStyles }
export default { styled, tailwindToStyle, registerThemeStyles }
